-r base.txt

# Exact pins — see base.txt. Here it also buys a reproducible test run: a Ruff or
# pytest minor picked up silently between two CI runs is a green build turning
# red (or the reverse) with no commit to blame it on.

# Testing
#
# pytest is on 9.x because 8.4.2 — the newest the old `>=8.0,<9.0` range allowed
# — carries PYSEC-2026-1845, whose fix ships in 9.0.3. Pinning is what surfaced
# it: the range's ceiling sat below the fix, and CI only audited
# requirements/production.txt, so nothing ever looked. pytest-cov follows to 7.x
# for the same major.
pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
factory-boy==3.3.3
time-machine==2.19.0

# Linting & formatting (Ruff replaces black + isort + flake8)
ruff==0.16.3
pre-commit==4.6.2

# PostgreSQL, so the suite can run on the engine production runs on.
#
# CI sets DATABASE_URL and development.py picks it up. This is not about parity
# for its own sake: on SQLite, Django reports `has_select_for_update = False` and
# silently drops the FOR UPDATE clause, so every row-locked path in the app was
# tested unlocked. SQLite also doesn't enforce CharField(max_length=N).
#
# Same versions as production.txt on purpose — testing against a different
# driver than the one that ships defeats the point.
dj-database-url==2.3.0
psycopg2-binary==2.9.12
