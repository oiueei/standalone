# Mixpanel (EU host) — pseudonymous product analytics. See MIXPANEL.md.
# Project token is safe to expose in the client bundle (Mixpanel design)
# but keep it out of git. Leave unset in dev to disable analytics entirely.
VITE_MIXPANEL_TOKEN=
